name: 🛡️ Daily Security Study & Update

on:
  schedule:
    - cron: '0 0 * * *' # 매일 한국 시간 09:00 (UTC 00:00)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-study:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 🧠 Generate Content & Update README
        run: |
          python study_generator.py

      - name: 🚀 Commit and Push
        run: |
          git config --global user.name "Security-Bot"
          git config --global user.email "bot@github.com"
          
          # 포스트 파일과 README 변경사항 모두 스테이징
          git add _posts/*.md README.md
          
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "📚 Daily Study Update: $(date +%Y-%m-%d)"
            git push
          fi
